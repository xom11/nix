# Shortcuts

> *Config keyboard in macos/linux*

---
## System
### Tab and Caplock behavior
- Remap `Tab` to `Cmd + Ctrl + Opt + Shift` when holding
- Remap `Caps Lock` to `Escape` when tapping
- Remap `Caps Lock` to `Cmd + Ctrl + Opt` when holding
### App launcher 
- `Cap + <char>` to run or focus app
### Clipboard manager
- `Cap + <number>` to copy to clipboard `<number>` (1-5)
- `Tab + <number>` to paste from clipboard `<number>`
- `Alt + V` to open clipboard history
- `Tab + ~` to area selection snapshot to clipboard 
### Arrow Keys and Home/End/PgUp/PgDn
- `Tab + H/J/K/L` to move left/down/up/right
- `Tab + Y/U/I/O` to remap to Home/PgDown/PgUp/End
### Audio and Brightness
- `Cap + plus/minus` to volume up/down
- `Cap + 0` to mute/unmute
- `Tab + plus/minus` to brightness up/down
### Power and Lock
- `Meta + Alt + l` to lock screen
- `Meta + Alt + Shift + l` to lock out
- `Meta + Alt + Shift + s` to shutdown
- `Meta + Alt + Shift + h` to hibernate
- `Meta + Alt + Shift + r` to reboot

