A lot of bug:
- Visual Studio Code : app name Code
- PWA : hard to find the bundle id
    Fix: using appscript
- Surfingkey/vimium: can not use shortcut when hide and focus
    Fix: using switch app instead of hide app
## How to use
- `Cap` + key to raise/focus/hide app
- `Cap` + `a` + key to raise/focus/hide extend app
